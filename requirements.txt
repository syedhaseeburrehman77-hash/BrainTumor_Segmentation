# FeTS 2022 Flower simulation environment (Python 3.11)
# Flower's simulation extra installs a compatible Ray runtime automatically.
flwr[simulation]==1.37.0

# For a CPU-only system install PyTorch first with:
# python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
monai>=1.3.0
nibabel>=5.2.0
numpy>=1.26.0,<3.0.0
pandas>=2.1.0
scipy>=1.11.0
